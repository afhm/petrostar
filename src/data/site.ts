/** Apex 308-redirects to www, so www is the canonical host. */
export const SITE_URL = 'https://www.petrostarin.com';
export const SITE_NAME = 'Petrostar Innovative';

/** Every indexable route. Drives the sitemap. */
export const PAGES = ['/', '/about', '/services', '/contact'] as const;
